version: "3.3"

services:
        flask-app:
                build: .
                container_name: flask_docker_app
                ports:
                        - "5001:5000"
                volumes:
                        - .:/app
                restart: always
